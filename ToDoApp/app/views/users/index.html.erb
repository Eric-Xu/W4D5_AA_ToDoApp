<h1>Welcome to the Members Page</h1>
<h3>Click on a user:</h3>
<ul>
	<% @users.each do |user| %>
		<li><%= link_to user.full_name, user_path(user.id) %></li>
	<% end %>
</ul>
<%= link_to "Add a new member", new_user_path %>