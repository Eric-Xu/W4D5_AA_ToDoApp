<h1>Welcome to the Teams Page</h1>
<h3>Existing Teams:</h3>
<ul>
	<% @teams.each do |team| %>
		<li><%= link_to team.name, team_path(team.id) %></li>
	<% end %>
</ul>
<%= link_to "Add a new team", new_team_path %>
